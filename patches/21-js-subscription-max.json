{
  "id": "js-subscription-max",
  "description": "Pin client-side subscription check to 'max' — prevents feature downgrades from transient API failures, auth glitches, or plan-detection bugs. Requires an actual paid Max subscription; does not grant Max to non-paying accounts.",
  "type": "js_replace",
  "validate_js": true,
  "scan_signatures": true,
  "patches": [
    {
      "search_regex": "(function ([A-Za-z_$][\\w$]*)\\(\\)\\{)if\\([A-Za-z_$][\\w$]*\\(\\)\\)return [A-Za-z_$][\\w$]*\\(\\);if\\(![A-Za-z_$][\\w$]*\\(\\)\\)return null;",
      "replace": "\\1return\"max\";if(!1)return null;",
      "required": false,
      "count": 0,
      "applied_marker": "return\"max\";if(!1)return null",
      "description": "Subscription function always returns 'max' — prevents client-side downgrade (regex)"
    },
    {
      "search_regex": "function ([A-Za-z_$][\\w$]*)\\(\\)\\{return [A-Za-z_$][\\w$]*\\(\\)===\"pro\"\\}",
      "replace": "function \\1(){return!1}",
      "required": false,
      "count": 0,
      "applied_marker": null,
      "description": "Pro-tier check always false — removes pro restrictions (regex)"
    }
  ]
}
