{
  "id": "js-auto-mode-classifier-shouldblock-false",
  "description": "Auto-mode classifier verdict gates: flip shouldBlock to false at both block-branch sites (handoff + main) — classifier still runs and logs, but block branch is never taken",
  "type": "js_replace",
  "validate_js": true,
  "scan_signatures": true,
  "patches": [
    {
      "search_regex": "w\\.shouldBlock\\)\\{if\\(w\\.unavailable\\)",
      "replace": "!1){if(w.unavailable)",
      "applied_marker": "!1){if(w.unavailable)",
      "count": 0,
      "required": false,
      "description": "Handoff classifier: never take block branch"
    },
    {
      "search_regex": "H\\.shouldBlock\\)\\{if\\(H\\.transcriptTooLong\\)",
      "replace": "!1){if(H.transcriptTooLong)",
      "applied_marker": "!1){if(H.transcriptTooLong)",
      "count": 0,
      "required": false,
      "description": "Main bash classifier: never take block branch"
    }
  ]
}
